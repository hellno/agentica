"use client";

import { useMiniKit } from "@worldcoin/minikit-js";

// Re-export the useMiniKit hook for convenient access throughout the app
export { useMiniKit };

// You can use this hook in any client component to access MiniKit functionality:
// const { isInstalled } = useMiniKit();
